for(int i=1;i*i<=arr[0];i++)
    {
     if(arr[0]%i==0)
     {
      rem.push_back(i);
      rem.push_back(arr[0]/i);
      if(i*i==arr[0])
      rem.pop_back();
     }
    }
    sort(rem.begin(),rem.end());
